export default ({
  route,
  store,
  redirect
}) => {
  // route.matched.forEach((item, index) => {
  //   item.meta.title = route.meta[index].title || '';
  // })
  // // route.matched.forEach((item, index) => {
  // //   item.meta.title = route.meta[index].title || '';
  // // })
  // // let title = route.meta.find(val => val.title != undefined);
  // // title ? route.title = title.title : "";
  // // store.commit('history/addHistory', route);
  // console.log(route)

  // if (route.path == "/maintenance") {
  //   console.log(store.state.webSet)
  //   store.state.webSet && store.state.webSet.webState && redirect("/")
  // }
}
